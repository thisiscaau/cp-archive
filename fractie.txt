/*                 thisiscaau's  code              
        What’s happened happened. Which is an expression of faith 
    in the mechanics of the world. It’s not an excuse to do nothing.
*/
/* shortcuts */
#include<bits/stdc++.h>
using namespace std;
#define ll int
#define fi first
#define se second
#define pb push_back
#define mp make_pair
typedef pair<ll,ll> ii;
typedef vector<ii> vii;
/* constants */
ll const mod = 100003, MAXN = 2e5 + 5;
/* declaration */
ll n,opt;
ll dp[2005][2005];
/* workspace */
signed main(){
    freopen("FRACTIE.inp","r",stdin);
    freopen("FRACTIE.out","w",stdout);
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cin >> opt >> n;
    if (opt == 1){
        /* mathforces stuffs */
        for (int i = 1 ; i < n ; i++){
            cout << i << " "; 
            if (i == n-1) cout << i;
        }
        return 0;
    }
    /* dp goes here */
    dp[2][1] = 1;
    for (int i = 3 ; i <= n ; i++){
        for (int j = 1 ; j <= i/2 ; j++){
            /* divide */
            dp[i][j] = dp[i][j-1] + dp[i-j][(i-j)/2] - dp[i-j][(j+1)/2 - 1];
            if (dp[i][j] > mod) dp[i][j] -= mod;
            else if (dp[i][j] < 0) dp[i][j] += mod;
        }
    }
    cout << dp[n][n/2] << endl;
}